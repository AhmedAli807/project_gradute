import 'package:flutter/material.dart';
import 'package:project_gradute/views/factory_map/widgets/factory_map_view_body.dart';

class FactoryMapView extends StatelessWidget {
  const FactoryMapView({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const FactoryMapViewBody();
  }
}
